import styles from './styles.module.scss';

const ContactList = () => {
  return (
    <div>
      <span className={styles.contact_list}>Contact List</span>
    </div>
  );
};

export default ContactList;
